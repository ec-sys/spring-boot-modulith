spring:
  application:
    name: sample-spring-modulith
  profiles:
    active: replication
management:
  tracing:
    sampling.probability: 1.0
    enabled: false
management.endpoints.web.exposure.include: "*"